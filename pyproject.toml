[project]
name = "zettelvault"
version = "0.1.0"
description = "Transform an Obsidian vault into PARA + Zettelkasten structure using DSPy and RLM"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dspy>=3.1.3",
    "pyyaml>=6.0",
]
